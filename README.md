Add a text to any .png or .jpeg picture using this command for the console written in C#.
Just download the file addText.exe into any folder and execute the file.
addText.exe admits two parameters: the .png file and the text you want to add to the picture.
Example:
addtext.exe background.png "javier928"
Result:
The text "javier928" will be placed in the picture.
Find the output.png file in your folder.
EXTRA TOOLS: [ pdfJoiner.exe joins two pdf files. file1.pdf and file2.pdf must be in the folder c:/dbase ]
[ createCover.exe creates a single A4 PDF named page0cover.pdf containing centered name AND surname. Name can be given as the first parameter in the command prompt. Surname can be given as a second parameter in the command prompt. "Courier" can be given as a third parameter. If no third parameter is given, Times New Roman will be used for generating the pdf file ]. 
[ pdf4.exe creates a single A4 PDF file (named centered.pdf or margin2cm.pdf) with a centered image you can choose. Unzip pdf4zip.7z and run pdf4.exe ]. [  JoinODPfiles.exe is a tool to join two presentations (odp.files) ] [ EjemploCS_CreateODP.exe creates a presentation with one slide called presentation.odp ].
